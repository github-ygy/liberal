
redis数据存储
    1.链表:压缩链表（适用用数据小，因为需要申请连续空间），双向链表
    2.字典:压缩列表，散列表（采用渐进式扩容缩容策略，节约时间，但是占用内存）
    3.有序集合:跳表
redis数据持久化
    清除原有的存储结构，只将数据存储到磁盘中，需要还原数据时，再重新将数据组织成原来的数据结构

如何保证缓存和数据库数据的一致性

redis怎么实现分布式锁，有什么缺陷

redis是怎么做内存优化的

redis淘汰策略有哪些

